# Lexsy_Assignment
A Flask web app that transforms static legal documents into dynamic, conversational forms. Users upload a .docx template, and the app uses a Generative AI to analyze placeholders and create a smart chat UI. The app guides you through filling in each field, then generates a completed, downloadable document.
